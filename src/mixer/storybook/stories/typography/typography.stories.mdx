import { Meta, Story, Canvas } from '@storybook/blocks';
import { Typography } from '@websolutespa/bom-mixer-ui';

<Meta
  title="Typography"
  decorators={[(story) => (
    <div style={ { padding: '0' } }>
      { story() }
    </div>
  )]}
  parameters={{
    backgrounds: { disable: true, grid: { disable: true } },
  }}
/>

# Typography

<Canvas>
  <Story name="Typography">
    <Typography />
  </Story>
</Canvas>
